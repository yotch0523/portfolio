export type MenuLink = {
  to: string;
  label: string;
}

export type ServiceLink = {
  name: string;
  icon: string;
  to: string;
  color: string;
}

export type Skill = {
  name: string;
  icon: string;
  color: string;
}